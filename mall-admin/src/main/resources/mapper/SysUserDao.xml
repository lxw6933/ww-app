<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ww.mall.mvc.dao.SysUserDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.ww.mall.mvc.entity.SysUserEntity" id="sysUserMap">
        <result property="id" column="id"/>
        <result property="centerId" column="center_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="isDel" column="is_del"/>
        <result property="version" column="version"/>
        <result property="createTime" column="create_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateBy" column="update_by"/>
    </resultMap>

    <insert id="insertUserAndRole">
        insert into sys_user_role (user_id,role_id) values
        <foreach collection="roleIds" separator="," item="item">
            (#{userId},#{item})
        </foreach>
    </insert>

    <delete id="removeUserAndRole">
        delete from sys_user_role where user_id = #{userId}
    </delete>

    <select id="queryUserOfRole" resultType="com.ww.mall.mvc.view.vo.admin.SysRoleVO">
        SELECT
            role.id,
            role.role_name,
            role.role_desc,
            role.role_no,
            role.role_sort,
            role.create_time,
            role.update_time
        FROM sys_user u
        RIGHT JOIN sys_user_role ur
        ON u.id = ur.user_id
        LEFT JOIN sys_role role
        ON ur.role_id = role.id
        WHERE u.username = #{username}
        AND u.is_del = false
    </select>

    <select id="queryUserOfPermission" resultType="com.ww.mall.mvc.view.vo.admin.SysPermissionVO">
        SELECT  p.id,
                p.name,
                p.type,
                p.url,
                p.code,
                p.perm_url,
                p.icon,
                p.sort,
                p.create_time,
                p.update_time
        FROM sys_permission p
        LEFT JOIN sys_role_permission rp
        ON p.id = rp.permission_id
        LEFT JOIN sys_role r
        ON r.id = rp.role_id
        LEFT JOIN sys_user_role ur
        ON r.id = ur.role_id
        LEFT JOIN sys_user u
        ON ur.user_id = u.id
        WHERE u.username = #{username}
        AND u.is_del = false
    </select>

</mapper>