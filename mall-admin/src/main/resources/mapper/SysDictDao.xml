<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ww.mall.mvc.dao.SysDictDao">
    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.ww.mall.mvc.entity.SysDictEntity" id="SysDictMap">
        <result property="id" column="id"/>
        <result property="dictType" column="dict_type"/>
        <result property="label" column="label"/>
        <result property="code" column="code"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="createBy" column="create_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="updateBy" column="update_by"/>
    </resultMap>

    <select id="page" parameterType="com.ww.mall.mvc.view.query.admin.SysDictQuery"
            resultType="com.ww.mall.mvc.entity.SysDictEntity">
        select
        id as id,
        dict_type as dictType,
        label as label,
        code as code,
        status as status
        from sys_dict
        <where>
            deleted = 1
            <if test="dictType != null and dictType != ''">
                and dict_type = #{dictType}
            </if>
            <if test="label != null and label != ''">
                and label = #{label}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="getDictValue" resultType="java.lang.String">
        select label
        from sys_dict
        where dict_type = #{type}
          and code = #{code}
          and status = true
          and deleted = 1
    </select>

    <select id="getDict" parameterType="java.lang.String" resultType="com.ww.mall.mvc.view.vo.admin.SysDictVO">
        select a.id,
               a.dict_type dictType,
               a.label,
               a.code,
               a.status
        from sys_dict a
                 left join sys_dict_type b
                           on a.dict_type = b.type
        where a.dict_type = #{dictType}
          and a.status = true
          and b.status = true
          and a.deleted = 1
        order by sort desc
    </select>

</mapper>
