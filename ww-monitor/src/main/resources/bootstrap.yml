spring:
  application:
    name: ww-monitor
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: ww-${spring.profiles.active}
        username: nacos
        password: nacos
