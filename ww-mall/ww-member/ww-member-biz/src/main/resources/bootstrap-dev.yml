grpc:
  server:
    port: 17000
    # 是否开启保持连接（长连接）
    enableKeepAlive: true
    # 保持连接时长（默认20s）
    keep-alive-timeout: 5s
    #表示当grpc连接没有数据传递时，多久之后开始向client发送ping packet
    keep-alive-time: 10s
    #是否允许客户端发送保持活跃的HTTP/2 ping，即使在连接上没有未完成的rpc。默认为false
    permit-keep-alive-without-calls: true
    # 10Mb 该参数控制服务端可以接收的最大消息大小。对于高并发场景，建议将其设置为较小的值，以减少网络延迟和内存占用
    max-inbound-message-size: 10485760

server:
  port: 19300  #服务端口号

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://192.168.10.141:3306/mall_basic?characterEncoding=UTF-8&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
#    url: jdbc:mysql://localhost:3306/ww_mall?characterEncoding=UTF-8&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://172.28.174.84:3307/ww_mall?characterEncoding=UTF-8&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: admin
#    password: adminlxw
  redis:
    host: 172.28.174.84
    password: 123456
    port: 6379
  data:
    mongodb:
      # 副本集群配置
      uri: mongodb://ww:admin@172.28.174.84:27001,172.28.174.84:27002,172.28.174.84:27003/ww_mall?replicaSet=rs&readPreference=secondaryPreferred&connectTimeoutMS=300000
      # 单机配置
      #      host: localhost:27017
      #      database: ww_mall
      #      username: admin
      #      password: admin
      #      authentication-database: admin
  rabbitmq:
    addresses: 172.28.174.84:5672
    virtual-host: /
    # 设置发送端到exchange确认回调
    publisher-confirm-type: correlated
    # 设置发送端到queue确认回调
    publisher-returns: true

# 开启sentinel feign支持【调用方（调用服务方）的熔断保护、被调用方（服务提供方）的降级保护】
feign:
  sentinel:
    enabled: true

# mybatis-plus配置
mybatis-plus:
  #  configuration:
  #配置字段驼峰
  #    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mapper/*.xml
  #实体扫描，多个package用逗号或者分号分隔
  type-aliases-package: com.ww.mall.member.entity
  global-config:
    db-config:
      #主键类型  AUTO:"数据库ID自增", INPUT:"用户输入ID", ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
      id-type: auto
      #字段策略 IGNORED:"忽略判断",NOT_NULL:"非 NULL 判断"),NOT_EMPTY:"非空判断"
      logic-delete-field: NOT_NULL
      logic-delete-value: 1
      logic-not-delete-value: 0
    banner: false
