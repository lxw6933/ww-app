server:
  port: 8000  #服务端口号
  servlet:
    context-path: /ww-third-server

# 开启sentinel feign支持【调用方（调用服务方）的熔断保护、被调用方（服务提供方）的降级保护】
feign:
  sentinel:
    enabled: true

edu:
  appId: demo
  security: c6agXWwt
  eduDomain: https://test-edu.founderyt.com
  reqUri: /apple/edu/onlineorder
  uploadImgUri: /apple/edu/image/upload
